What is JSX, and why is it used?

ans:JSX (JavaScript XML) is a syntax that lets you write HTML-like code inside JavaScript.

Why used:HTML inside JS for building React UI faster and cleaner.

2..What is the difference between State and Props

ans props is a External read-only data (input to a component).
state is a Internal changeable data (managed by the component).

3..What is the useState hook, and how does it work?
ans:useState is a React hook used to add state to functional components.

It allows a component to remember values between renders and update the UI when the state changes.

4...How can you share state between components in React?


ans:State can be shared between components by lifting it up to a common parent or using Context/Redux for global state.

5...How is event handling done in React?

ans:In React events are handled by passing a function to an element’s  like onClick.